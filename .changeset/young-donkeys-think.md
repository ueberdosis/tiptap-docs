---
'tiptap-docs': patch
---

Added `anchorToSelection` prop documentation to `<AiMenu />` and `<AiMenuContent />` prop tables. Updated `useAiContentTracker` and `useTextSelectionTracker` hook docs with full parameter tables and new call signatures. Added documentation for new utilities: `getSelectionRangeRect`, `createEditorWidthAnchorRect`, `createVirtualAnchor`, and `getEditorContentRect`.