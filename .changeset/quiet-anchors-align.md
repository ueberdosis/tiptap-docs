---
'tiptap-docs': patch
---

Correct the DOCX import documentation: internal anchor links and bookmark targets are documented as supported now that they round-trip, the custom node and mark mapping tables use the real case-sensitive names and the correct key direction, and the import extension lists all of its options.
