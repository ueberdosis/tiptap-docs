---
'tiptap-docs': patch
---

Update move node button documentation:

- Added missing imports to `useMoveNode()`
- Added parameters section to `canMoveNode()` utility function
- Added parameters section to `moveNode()` utility function
- Added parameters section to `shouldShowButton()` utility function