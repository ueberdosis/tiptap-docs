---
'tiptap-docs': patch
---

Document what the DOCX import sends to your image upload endpoint: the 10 second timeout, up to three uploads at a time, the image types it forwards, that there is no size or count limit, and that a failed upload is skipped without a retry.
