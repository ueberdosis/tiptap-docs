---
'tiptap-docs': patch
---

Document the new `headerFooterOverrides` option on the DOCX export extension and update the `paragraphOverrides` docs to reflect that `spacing.before` and `spacing.after` are now preserved while `spacing.line` is computed from each paragraph's `lineHeight`.