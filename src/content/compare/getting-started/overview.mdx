---
title: Compare
tags:
  - type: addon
  - type: alpha
meta:
  title: Compare | Tiptap Docs
  description: Compare Tiptap documents and versions and display the changes in the editor.
  category: Compare
sidebars:
  hideSecondary: true
---

import { Callout } from '@/components/ui/Callout'
import { CodeDemo } from '@/components/CodeDemo'
import Link from '@/components/Link'

With Tiptap Compare, you can find and display the changes between two documents or two versions of the same document.

<CodeDemo isPro path="" src="https://deploy-preview-910--tiptap-pro.netlify.app/preview/Extensions/CompareDocuments" />

## Install

First, <a href="https://tiptap.dev/contact-sales?form=ai-toolkit">contact our team</a> to get early access to Tiptap Compare.

After gaining access, configure your package manager by following the [private registry
guide](/guides/pro-extensions).

Then, install the package:

```bash
npm install @tiptap-pro/compare
```

Add the `Compare` extension to the editor that displays the changes between the two documents.

```ts
import { Compare } from '@tiptap-pro/compare'

const editor = new Editor({
  extensions: [StarterKit, Compare],
})
```

## Use cases

With Tiptap Compare, you can find the changes between two documents or two versions of the same document.

<div className="grid md:grid-cols-2 gap-6 mt-6 md:items-start">
  <Link
    href="/compare/guides/compare-documents"
    className="flex flex-col p-4 border border-grayAlpha-200 rounded-lg hover:border-grayAlpha-300 transition-colors cursor-pointer no-underline"
  >
    <div className="flex items-center justify-between mb-2">
      <h4 className="font-semibold text-black">Compare documents</h4>
      <span className="text-sm text-blue-600 font-medium whitespace-nowrap ml-4">More →</span>
    </div>
    <div className="text-sm text-grayAlpha-700">
      Compare two documents. Find what changed and preview the differences.
    </div>
  </Link>
  <Link
    href="/compare/guides/compare-versions"
    className="flex flex-col p-4 border border-grayAlpha-200 rounded-lg hover:border-grayAlpha-300 transition-colors cursor-pointer no-underline"
  >
    <div className="flex items-center justify-between mb-2">
      <h4 className="font-semibold text-black">Compare versions</h4>
      <span className="text-sm text-blue-600 font-medium whitespace-nowrap ml-4">More →</span>
    </div>
    <div className="text-sm text-grayAlpha-700">
      Compare two different versions of a collaborative document.
    </div>
  </Link>
</div>
