---
title: Page break background
meta:
  title: Page Break Background | Tiptap Pages Docs
  description: Learn how to change the background color between pages in the Tiptap Pages extension.
  category: Pages
---

import { Callout } from '@/components/ui/Callout'

The `pageBreakBackground` option lets you change the background color of the area between pages. This can help visually separate pages or match your app’s background color design.

## How to set the page break background

```js
Pages.configure({
  pageBreakBackground: '#f8f8f8', // Light gray between pages
})
```

This only affects the space between pages, not the page itself.

<Callout title="Design tip">
  Use a subtle color for the page break background so it doesn’t distract from your content.
</Callout> 