---
title: Available Methods
meta:
  title: Available Methods | Tiptap Content AI
  description: Learn about the available methods in the AI Agent provider.
  category: Content AI
---

The AI Agent provider exposes several methods that you can call to control the AI Agent's behavior and manage the conversation.

## Core methods

| Method                          | Description                                                                      | Parameters                       |
| ------------------------------- | -------------------------------------------------------------------------------- | -------------------------------- |
| `run()`                         | Starts a new AI Agent run                                                        | None                             |
| `stop()`                        | Stops the current AI Agent run                                                   | None                             |
| `reset(chatMessages?)`          | Resets the AI Agent provider to its initial state                                | `chatMessages?`: `ChatMessage[]` |
| `setSystemPrompt(systemPrompt)` | Sets the [system prompt](/content-ai/capabilities/agent/configure/system-prompt) | `systemPrompt`: `string \| null` |

## Conversation management

| Method                              | Description                                                                                                 | Parameters                                               |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| `addUserMessage(message, options?)` | Adds a user message to the conversation                                                                     | `message`: `string`, `options?`: `AddUserMessageOptions` |
| `setChatMessages(messages)`         | Replaces all messages in the conversation                                                                   | `messages`: `ChatMessage[]`                              |
| `setAutoAccept(autoAccept)`         | Changes the [auto-accept](/content-ai/capabilities/agent/features/review#auto-accept-configuration) setting | `autoAccept`: `"always" \| "never" \| "onlyRead"`        |
| `setCheckpoint()`                   | Saves the current state as a checkpoint                                                                     | None                                                     |
| `restoreCheckpoint(checkpoint)`     | Restores a previously saved checkpoint                                                                      | `checkpoint`: `Checkpoint`                               |

## Tool call review

| Method             | Description                   | Parameters |
| ------------------ | ----------------------------- | ---------- |
| `acceptToolCall()` | Accepts the pending tool call | None       |
| `rejectToolCall()` | Rejects the pending tool call | None       |
