---
title: Table fields example
tags:
  - type: beta
  - type: restricted
meta:
  title: Table fields example | Tiptap Blocks Docs
  description: Fill table fields and add or remove body rows while keeping headers and columns fixed.
  category: Blocks
---

import { CodeDemo } from '@/components/CodeDemo'

Fill the slots in each cell, or add and remove body rows. The headers and three columns stay fixed, and the document cannot contain anything outside the table.

<CodeDemo isPro path="/Examples/TableFields" />

The example combines Slots and Content Protection with a table-only document schema. Each body cell contains one Slot. The policy protects headers and field definitions; table rules preserve the fixed columns when rows are inserted or pasted.
