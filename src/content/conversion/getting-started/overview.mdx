---
title: Document import and export
tags:
  - type: start
  - type: beta
meta:
    title: Document Import and Export | Tiptap Conversion — DOCX, PDF, Markdown
    description: Import DOCX and Markdown into your Tiptap editor, export to DOCX, PDF, ODT, EPUB, and more. Learn how the conversion pipeline works and what extensions you need.
    category: Conversion
---

import { Callout } from '@/components/ui/Callout'
import { EnterpriseCalloutAuto } from '@/components/EnterpriseCalloutAuto'

<EnterpriseCalloutAuto variant="docx" renderMode="sidebar" />

Tiptap Conversion translates between document formats and your editor's document model. It supports importing DOCX and Markdown, and exporting to DOCX, PDF, ODT, EPUB, and Markdown. It is not a document renderer. It is a bridge. What renders in your editor depends on which extensions you have installed.

<Callout title="Pro package" variant="hint">
  Conversion is a Pro package included with all [Tiptap subscriptions](https://tiptap.dev/pricing). Before installation, you'll need to set up [access to Tiptap's private NPM
  registry](/guides/pro-extensions).
</Callout>

## The conversion pipeline

Conversion is a multi-step pipeline, not a single operation. The conversion service handles the translation between file formats and Tiptap's JSON document model. Your editor handles the rendering.

1. **Import:** The conversion service parses your source document and produces Tiptap JSON, a structured representation of the document's content, formatting, and layout.
2. **Editor rendering:** Your Tiptap editor receives the JSON. For each node type in the JSON (heading, table, image, etc.), the editor needs a matching extension installed to render it. Content without a matching extension needs to be managed. See the [invalid schema guide](/guides/invalid-schema) for strategies.
3. **Export:** The export extension converts your editor's content back into a document format. It exports what the editor currently contains, not what the original source document had.

See the [Supported features](/conversion/getting-started/feature-support-matrix) page for a full breakdown of what works at each stage, or explore the [Content types](/conversion/content-types/text-and-formatting/paragraphs) pages for detailed per-feature documentation.

Try it yourself in the [live demo](/conversion/getting-started/demo).

## What you need to render converted content

How much of the document experience you recreate in the editor depends on what you are building.

### Core extensions

For most content like text, headings, lists, tables, images, links, and formatting, standard Tiptap extensions handle the rendering. `StarterKit` covers the basics. Add `TableKit`, `Image`, `TextAlign`, and text styling extensions for broader coverage.

This is enough if you are building a **content editor** or **CMS** that accepts document uploads. The text content renders correctly in a continuous-scroll editor. For Markdown imports, core extensions are typically all you need.

The [Supported features](/conversion/getting-started/feature-support-matrix) page shows which extensions you need for each feature and whether they are included in StarterKit.

### Pages extension for document layout

For page-aware documents with headers, footers, page breaks, page numbering, page size and margins, you need the [Pages extension](/pages/getting-started/overview). It provides a paginated editing experience where content flows across visual pages, and headers and footers have dedicated editing areas.

This is what you need if you are building a **document editor** where users expect a page layout. Without Pages, imported headers and footers have nowhere to render, page breaks are ignored, and there is no concept of page boundaries.

| What you are building | What you need |
| --- | --- |
| Content editor or CMS with document upload | Core extensions (StarterKit + formatting) |
| Document editor with page layout | Core extensions + [Pages extension](/pages/getting-started/overview) |
| Headless pipeline (no editor) | REST API only, no extensions needed |

## Supported formats

You can integrate conversion using editor extensions (commands within your Tiptap editor) or the REST API (HTTP endpoints for server-side or non-editor use).

| Format | Import | Export | Notes |
| --- | --- | --- | --- |
| **DOCX** (Word) | [Extension](/conversion/import/docx/editor-extension), [REST API](/conversion/import/docx/rest-api) | [Extension](/conversion/export/docx/editor-extension), [REST API](/conversion/export/docx/rest-api) | |
| **Markdown** | [REST API](/conversion/import/markdown/rest-api) | [Extension](/conversion/export/markdown/editor-extension), [REST API](/conversion/export/markdown/rest-api) | |
| **PDF** | | [Extension](/conversion/export/pdf/editor-extension), [REST API](/conversion/export/pdf/rest-api) | |
| **ODT** | | [Extension](/conversion/export/odt/editor-extension), [REST API](/conversion/export/odt/rest-api) | |
| **EPUB** | | [Extension](/conversion/export/epub/editor-extension), [REST API](/conversion/export/epub/rest-api) | |
| **DOC** (legacy) | | [Extension](/conversion/export/doc/editor-extension), [REST API](/conversion/export/doc/rest-api) | |

Markdown is also available as a [core editor feature](/editor/markdown) with built-in parsing and serialization, independent of the conversion service.

## Next steps

- [Supported features](/conversion/getting-started/feature-support-matrix) to see what works across import, editor, and export
- [Install](/conversion/getting-started/install) to set up credentials and install extensions
- [Live demo](/conversion/getting-started/demo) to try DOCX import and export in the browser

## Legacy Import/Export extensions

The legacy `@tiptap-pro/extension-import` and `@tiptap-pro/extension-export` packages are being deprecated and will be sunset in 2026. These extensions receive no new features and are maintained for existing users only.

- [Legacy DOCX Import/Export](/conversion/legacy/overview)
- [Legacy ODT Import/Export](/conversion/legacy/odt/editor-extensions)
- [Legacy Markdown Import/Export](/conversion/legacy/markdown/editor-extensions)

**Migrating to the new extensions:** The current [Import DOCX](/conversion/import/docx/editor-extension) and [Export DOCX](/conversion/export/docx/editor-extension) extensions provide improved conversion quality, more format options, and active development. We recommend migrating as soon as possible.
