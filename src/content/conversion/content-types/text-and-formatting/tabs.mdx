---
title: Tabs
tags:
  - type: beta
meta:
  title: Tabs | Tiptap Conversion
  description: How tab characters from Word are handled during DOCX import and what developers should expect.
  category: Conversion
---

import { Callout } from '@/components/ui/Callout'

Word documents use tab characters for alignment, indentation, and columnar layout within paragraphs. Tiptap and HTML do not have a native tab stop concept, so tabs are converted to spaces during import.

## What you need

- **Extensions:** No additional extensions required. Tab content is converted to plain text.
- **Configuration:** None.

## Support overview

| | Import | Editor | Export |
| --- | --- | --- | --- |
| Tab characters | Converted to four spaces | Plain text (spaces) | Spaces remain as spaces |
| Tab stop positions | Not preserved | Not supported | Not supported |

## Import

Import tabs using the [editor extension](/conversion/import/docx/editor-extension) or the [REST API](/conversion/import/docx/rest-api). Both handle tabs identically.

When the import converter encounters a `<w:tab/>` element in a text run, it replaces it with four space characters. Word's tab stop positions (left, center, right, decimal, and bar tabs defined in paragraph or style properties) are not extracted or preserved.

## Editor rendering

Since tabs are converted to spaces on import, the editor renders them as regular text. There is no tab stop or tab character concept in Tiptap.

## Export

Spaces remain as spaces in the exported DOCX. There is no conversion from spaces back to tab characters. Content that originally used tabs in Word will export with spaces instead.
