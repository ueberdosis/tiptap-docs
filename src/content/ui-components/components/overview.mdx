---
title: Components overview
meta:
  title: Components | Tiptap UI Component
  description: Pick from a list of UI components and primitives to integrate into your Tiptap editor. More in the docs.
  category: UI Components
sidebars:
  hideSecondary: true
---

import { Callout } from '@/components/ui/Callout'

<Callout title="Upgrade to v3" variant="warning">
  To use UI components or templates you need to upgrade to Editor version 3.x. See the [upgrade
  guide](https://tiptap.dev/docs/resources/whats-new) for more details.
</Callout>

Components and Node Components offer prebuilt functionality for integrating with Tiptap’s features (with
node components being elements within the editor content), while primitives are the fundamental, low-level
UI pieces used as the backbone for building these richer interactions.

import { UIComponents } from '@/components/UIComponents'

<UIComponents path="content/ui-components" />
