---
title: Add a slash dropdown menu to insert content types
meta:
  title: Slash menu | Tiptap UI Component
  description: Integrate a slash dropdown menu that inserts different content types into Tiptap. More in the docs!
  category: UI Components
---

import { Callout } from '@/components/ui/Callout'

<Callout title="Upgrade to v3" variant="warning">
  To use UI components or templates you need to upgrade to Editor version 3.x. See the [upgrade
  guide](https://tiptap.dev/docs/resources/whats-new) for more details.
</Callout>

## Install

```bash
npx @tiptap/cli add slash-dropdown-menu
```

## Usage

```tsx
import { SlashDropdownMenu } from '@/components/tiptap-ui/slash-dropdown-menu'
import { useEditor, EditorContent } from '@tiptap/react'

export default function MyEditor() {
  const editor = useEditor({
    // editor configuration
  })

  return (
    <div>
      <EditorContent editor={editor} />
      <SlashDropdownMenu editor={editor} />
    </div>
  )
}
```

## Props

| Name            | Type           | Default                      | Description                |
| --------------- | -------------- | ---------------------------- | -------------------------- |
| editor          | Editor \| null | null                         | The Tiptap editor instance |
| floatingOptions | object         | -                            | Options for floating UI    |
| selector        | string         | 'tiptap-slash-dropdown-menu' | Selector for the target    |
| char            | string         | '/'                          | Slash character            |

## Requirements

Used reference components

- `tiptap-utils` (lib)
- `button` (primitive)
- `suggestion-menu` (ui)
- `heading-one-icon` (icon)
- `heading-two-icon` (icon)
- `code-block-icon` (icon)
- `list-icon` (icon)
- `list-ordered-icon` (icon)
- `image-icon` (icon)
- `heading-three-icon` (icon)
