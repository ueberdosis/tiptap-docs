---
title: Display and interact with comment threads
meta:
  title: Thread | Tiptap UI Component
  description: Interact and display comment threads in your Tiptap editor with the Thread UI component. More in the docs.
  category: UI Components
---

import { Callout } from '@/components/ui/Callout'

<Callout title="Upgrade to v3" variant="warning">
  To use UI components or templates you need to upgrade to Editor version 3.x. See the [upgrade
  guide](https://tiptap.dev/docs/resources/whats-new) for more details.
</Callout>

Components for displaying and interacting with comment threads.

## Install

```bash
npx @tiptap/cli add thread
```

## Usage

```tsx
import { Thread, ThreadFloating } from '@/components/tiptap-ui/thread'
import { useEditor, EditorContent } from '@tiptap/react'
import { Comments } from '@tiptap-pro/extension-comments'

export default function MyEditor() {
  const editor = useEditor({
    extensions: [
      // other extensions
      Comments.configure({
        // comments configuration
      }),
    ],
  })

  return (
    <div>
      <EditorContent editor={editor} />
      <ThreadFloating editor={editor} />
    </div>
  )
}
```

## Props

| Name   | Type           | Default | Description                |
| ------ | -------------- | ------- | -------------------------- |
| editor | Editor \| null | null    | The Tiptap editor instance |

## Requirements

Used reference components

- `app-context` (context)
- `collab-context` (context)
- `thread-store` (context)
- `user-context` (context)
- `use-mobile` (hook)
- `use-thread-handlers` (hook)
- `use-tiptap-editor` (hook)
- `use-thread-position` (hook)
- `use-thread-sync` (hook)
- `comments` (ui)
- `floating-element` (ui)
- `check-icon` (icon)
- `chevron-left-icon` (icon)
- `chevron-right-icon` (icon)
- `circle-check-icon` (icon)
- `close-icon` (icon)
- `ellipsis-icon` (icon)
- `link-icon` (icon)
- `rotate-ccw-icon` (icon)
- `trash-icon` (icon)
- `button` (primitive)
- `badge` (primitive)
- `dropdown-menu` (primitive)

Open source feature(s)

- `@floating-ui/react`

Cloud feature(s)

- `@hocuspocus/provider`
