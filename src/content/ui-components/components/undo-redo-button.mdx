---
title: Undo and redo editor actions button
meta:
  title: Undo Redo Button | Tiptap UI Component
  description: Integrate a button that helps undo and redo editor actions for your Tiptap editor. More in the docs
  category: UI Components
component:
  name: Un/redo button
  description: Undo or redo an action with a button
  type: component
  isFree: true
  isCloud: false
  isOpen: true
  isNew: false
  icon: Undo2
---

import { CodeDemo } from '@/components/CodeDemo'
import { Callout } from '@/components/ui/Callout'

<Callout title="Upgrade to v3" variant="warning">
  To use UI components or templates you need to upgrade to Editor version 3.x. See the [upgrade
  guide](https://tiptap.dev/docs/resources/whats-new) for more details.
</Callout>

A button for undoing and redoing editor actions.

<CodeDemo
  isScrollable
  isSmall
  src="https://template.tiptap.dev/preview/tiptap-ui/undo-redo-button"
/>

## Install

You can add the component via Tiptap CLI (for Vite or Next.js)

```bash
npx @tiptap/cli add undo-redo-button
```

### Manual Integration

For frameworks other than Vite or Next.js, add the component manually from the [open-source repository](https://github.com/ueberdosis/tiptap-ui-components/tree/main/apps/web/src/components/tiptap-ui/undo-redo-button).

### Import styles

This component requires you to import our styles which were added to `styles/keyframe-animation.scss` and `styles/_variables.scss`.

## Usage

```tsx
import { EditorContent, EditorContext, useEditor } from '@tiptap/react'
import { StarterKit } from '@tiptap/starter-kit'
import { UndoRedoButton } from '@/components/tiptap-ui/undo-redo-button'

import '@/components/tiptap-node/paragraph-node/paragraph-node.scss'

export default function MyEditor() {
  const editor = useEditor({
    immediatelyRender: false,
    extensions: [StarterKit],
    content: `
        <p>Try typing something and then click the undo and redo buttons.</p>
        `,
  })

  return (
    <EditorContext.Provider value={{ editor }}>
      <div className="tiptap-button-group" data-orientation="horizontal">
        <UndoRedoButton action="undo" />
        <UndoRedoButton action="redo" />
      </div>

      <EditorContent editor={editor} role="presentation" />
    </EditorContext.Provider>
  )
}
```

## Props

| Name   | Type             | Default  | Description                        |
| ------ | ---------------- | -------- | ---------------------------------- |
| editor | Editor \| null   | null     | The Tiptap editor instance         |
| action | 'undo' \| 'redo' | required | The history action to perform      |
| text   | string           | -        | Text to display alongside the icon |

## Requirements

Used reference components

- `use-tiptap-editor` (hook)
- `button` (primitive)
- `undo2-icon` (icon)
- `redo2-icon` (icon)
