---
title: Content Protection
tags:
  - type: beta
  - type: restricted
meta:
  title: Content Protection | Tiptap Docs
  description: Control which parts of a Tiptap document users can edit or read.
  category: Content Protection
---

Content Protection controls which parts of a document users can edit or read. Define permissions for Nodes, Marks, attributes and content with a policy.

## Install

First, [contact our team](https://tiptap.dev/contact-sales?form=pilot-program) for access to the Content Protection pilot program.

After gaining access, follow the [private registry setup guide](/guides/pro-extensions), then install the package:

```bash
npm install @tiptap-pro/extension-content-protection
```

Add the extension to your editor.

```ts
import { ContentProtection } from '@tiptap-pro/extension-content-protection'

const editor = new Editor({
  extensions: [ContentProtection.configure({ policy })],
})
```

Provide a [policy](/content-protection/api-reference/policy) that defines what can be edited.

Content protection is applied only client-side. For server-side content protection, [contact us](https://tiptap.dev/contact-sales?form=pilot-program).

See the [API reference](/content-protection/api-reference/extension) and [policy language](/content-protection/api-reference/policy).
